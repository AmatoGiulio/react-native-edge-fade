{
  "extends": "../tsconfig",
  "compilerOptions": {
    // Keep the existing example on RN's compatibility typings during the
    // SDK 58 preview migration. The library is checked against the public API.
    "customConditions": ["react-native-legacy-deep-imports"],
    // Feature-based source layout under src/, addressed with the `@/` alias.
    // Paths resolve relative to this tsconfig (no baseUrl needed on TS 5+);
    // Metro resolves the same tsconfig paths automatically on Expo SDK 49+.
    "paths": {
      "@/*": ["./src/*"],
      "react-native-edge-fade": ["../src/index"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ],
  "exclude": ["node_modules", "android", "ios", ".expo"]
}
